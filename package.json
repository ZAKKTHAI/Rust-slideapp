{
  "name": "slideapp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "present": "cargo run --manifest-path src-tauri/Cargo.toml -- present",
    "new": "cargo run --manifest-path src-tauri/Cargo.toml -- new",
    "import": "cargo run --manifest-path src-tauri/Cargo.toml -- import"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0",
    "@digital-go-jp/tailwind-theme-plugin": "^0.3.4"
  }
}
